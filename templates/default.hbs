{{#> basePartial}}
<article>
    <header>
        <h1><a href=".">{{title}}</a></h1>
        {{> datePartial}}
    </header>
{{{contents}}}
<footer>
    <p>More posts tagged: <a href="{{rootPath}}{{categoryPath}}">{{category}}</a></p>
</footer>
</article>
{{/basePartial}}
